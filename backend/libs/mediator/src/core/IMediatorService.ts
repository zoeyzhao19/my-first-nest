// import type { IRequest } from './IRequest';

// export interface IMediatorService {
//   send<T extends IRequest<any>>(
//     command: T,
//   ): Promise<T extends IRequest<infer R> ? R : never>;
// }

// export const IMediatorService = Symbol("IMediatorService");